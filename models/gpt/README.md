# GPT / Codex Notes

공통 지침은 루트 `AGENTS.md`를 따른다.

## 용도

- Codex/GPT 계열 도구의 코드 작업 기준
- GPT 모델별 프롬프트, 파라미터, 업그레이드 메모

## 메모

- Codex/GPT 계열에서는 `AGENTS.md`를 주 진입점으로 본다.
- 코드 변경 전 `AGENTS.md`, 관련 `agent.yaml`, `runner.py` 순서로 확인한다.
